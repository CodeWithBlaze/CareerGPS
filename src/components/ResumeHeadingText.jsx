import React from 'react';
import '../css/resume_text.css';
function ResumeHeadingText({text}) {
    return (
        <p className='resume-heading-text'>{text}</p>
    );
}

export default ResumeHeadingText;